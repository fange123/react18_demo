'use strict';

/**
 * staff-info-manager service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::staff-info-manager.staff-info-manager');
